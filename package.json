{
  "name": "textlint-rule-no-start-duplicated-conjunction",
  "repository": {
    "type": "git",
    "url": "https://github.com/azu/textlint-rule-no-start-duplicated-conjunction.git"
  },
  "author": "azu",
  "email": "azuciao@gmail.com",
  "homepage": "https://github.com/azu/textlint-rule-no-start-duplicated-conjunction",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/azu/textlint-rule-no-start-duplicated-conjunction/issues"
  },
  "version": "1.0.2",
  "description": "textlint rule that check no start with duplicated conjunction.",
  "main": "lib/no-start-duplicated-conjunction.js",
  "files": [
    "lib",
    "src"
  ],
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build",
    "test": "mocha"
  },
  "keywords": [
    "textlint"
  ],
  "devDependencies": {
    "babel": "^5.8.23",
    "espower-babel": "^3.3.0",
    "mocha": "^2.3.2",
    "power-assert": "^1.0.0",
    "textlint": "^3.3.2"
  }
}
